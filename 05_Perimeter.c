#include <stdio.h>
int main()
{
    int length=6;
    int breadth=10;
    int Perimeter=2*(length+breadth);
    printf("The rectangle with length %d and breadth %d has Perimeter=%d",length,breadth,Perimeter);
    return 0;
}

