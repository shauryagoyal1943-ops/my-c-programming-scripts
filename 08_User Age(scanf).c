//Input 
#include <stdio.h>
int main()
{
    int age;                                // Define Age Variable
    printf("Enter your age:");          // Ask user to enter age
    scanf("%d",&age);               // Read user input and store in age variable
    printf("Your age is %d",age);   // Print the age entered by user
    return 0;                   // Return 0 to indicate successful execution
}
//Program to print the age of user in C programming language.