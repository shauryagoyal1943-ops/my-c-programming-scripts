#include <stdio.h>
int main()
{
    int length=12;
    int breadth=23;
    int area=length*breadth;
    printf("The rectangle with length %d and breadth %d has area=%d",length,breadth,area);
    return 0;
}
